from django.apps import AppConfig


class {{ camel_case_app_name }}Config(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "{{cookiecutter.project_slug}}.{{ app_name }}.infrastructure.django"
    label = "{{ app_name }}"
